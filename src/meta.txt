create empty screenshot field for each result
if link in linkstoscreenshot && !screenshot: display loading 
push all normally
component level: if 


if(result.link in linkstoscreenshot){
  if(result[i].link === screenshot.site){
    push()
  }
  else if(!screenshot){
    push()
  }
}